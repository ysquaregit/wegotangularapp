import { ComponentRef } from '@angular/core';
import { IModalDialog, IModalDialogOptions } from './modal-dialog.interface';
export declare class SimpleModalComponent implements IModalDialog {
    text: string;
    dialogInit(reference: ComponentRef<IModalDialog>, options?: IModalDialogOptions): void;
}
